<UserControl
    x:Class="LayoutTest_UWP.UserCtrl.UC_LbItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LayoutTest_UWP.UserCtrl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="200">

    <Grid>
        <StackPanel Orientation="Horizontal">
            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="{Binding ImageText}" FontSize="{Binding ImageTextSize}" Name="fi"/>
            <TextBlock FontSize="36" Margin="10,0,0,0" Text="{Binding Content}"/>
        </StackPanel>
    </Grid>
</UserControl>
