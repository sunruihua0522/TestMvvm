<UserControl x:Class="TestRelativeBinding.MyListBoxItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestRelativeBinding"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="30" ToolTip="{Binding Name}" Width="200"/>
                <TextBlock Grid.Column="1" Text="{Binding ID}" FontSize="30" ToolTip="{Binding ID}" Width="200"/>
                <TextBlock Grid.Column="2" Text="XXXX" FontSize="30"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
